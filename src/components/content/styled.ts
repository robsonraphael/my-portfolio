import styled from "styled-components";
import { motion } from "framer-motion";

export const Container = styled.main``;
export const Title = styled(motion.h1)``;
export const Ani = styled(motion.p)``;